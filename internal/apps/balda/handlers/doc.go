// Package handlers owns Balda ingress behavior.
//
// It parses inbound transport requests, performs auth and session checks, and
// publishes work into actor/runtime workflows. It does not own product
// execution logic.
package handlers
