version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: fast
  disable:
    - gomoddirectives
    - wsl
    - wsl_v5
    - depguard
    - nlreturn
    - lll
    - mnd
    - funlen
    - gocognit
    - cyclop
    - nestif
    - godoclint
    - testpackage
    - interfacebloat
    - gocyclo
    - maintidx
    - embeddedstructfieldcheck
    - funcorder
    - tagalign
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - misspell
    - unconvert
    - goconst
    - gocritic
    - whitespace
    - nolintlint
    - forbidigo
  settings:
    forbidigo:
      analyze-types: true
      forbid:
        - pattern: '^state\.New(SQLite|Postgres)Provider$'
          pkg: '^github\.com/baldaworks/balda/internal/apps/balda/state$'
          msg: 'Use state.Open with resolved database configuration in production composition.'
    revive:
      rules:
        - name: exported
          disabled: true
  exclusions:
    rules:
      - path: 'internal/apps/balda/state/|_test\.go$'
        linters: [forbidigo]

formatters:
  enable: []
  settings: {}

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
